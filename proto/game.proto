syntax = "proto3";
package game;

enum GameType {
  GAME_TYPE_UNSPECIFIED = 0;
  GAME_TYPE_TIC_TAC_TOE = 1;
  GAME_TYPE_CHESS = 2;
}

message Position {
  uint32 row = 1;
  uint32 col = 2;
}

message PositionPair {
  Position first = 1;
  Position second = 2;
}
